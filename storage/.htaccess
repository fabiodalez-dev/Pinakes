# Deny all access to storage directory (logs, cache, temp files)

# Apache 2.4
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

# Apache 2.2 (fallback)
<IfModule !mod_authz_core.c>
    Order Deny,Allow
    Deny from all
</IfModule>
